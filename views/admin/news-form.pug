extends ../layout

block content
  h1= title

    if error.error !== undefined
      each message in error.error
          p=message

    form(method='post')
      p
        label Tytuł
        input(type='text' name='title' value=body.title || '')

      p
        label Opis
        textarea(name='description')=body.description || ''
      p
        input(type='submit' value='Zapisz')